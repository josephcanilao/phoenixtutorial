<h1>Listing Users</h1>

<table class="table">
	<th>Name | username | password</th>
	<th>action</th>
	<%= for user <- @users do %>
		
		<tr>
			<td><%= render "user.html", userindi: user %></td>
			<td><%= link "View", to: user_path(@conn, :show, user.id) %></td>
		</tr>

	<% end %>
</table>